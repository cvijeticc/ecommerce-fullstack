# Kopiraj ovaj fajl u .env i po potrebi promeni vrednosti:
#   cp .env.example .env
#
# Vite ubacuje u frontend build SAMO varijable koje počinju sa VITE_.
# Sve ovde završi u JavaScript bundle-u koji je vidljiv u browseru —
# NIKADA ne stavljaj lozinke, API ključeve ili JWT tajne u ovaj fajl.

# Adresa Spring Boot backenda
VITE_API_URL=http://localhost:8080/api
